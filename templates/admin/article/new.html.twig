{% extends 'admin.html.twig' %}

{% block title %}{{ 'Create new article' }}{% endblock %}

{% block body %}
    <h1>{{ 'Create new article' }}</h1>

    {{ include('admin/article/_form.html.twig') }}

    <a href="{{ path('app_admin_articles') }}">{{ 'Back' }}</a>
{% endblock %}
